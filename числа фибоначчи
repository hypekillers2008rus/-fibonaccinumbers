int Fib(int N)
{
  if (N == 1 || N == 2)
    return 1;
  return Fib(N - 1) + Fib(N - 2);
}
int main()
{
  int N;
  printf("N=");
  scanf("%d", &N);
  for (int i = 1; i <= N; i++)
    printf("%d ", Fib(i));
  getchar(); getchar();
  return 0;
}